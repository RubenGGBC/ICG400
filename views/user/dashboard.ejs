<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Dashboard - ICG400</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar', { user: user }) %>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h1>¡Bienvenido, <%= user.username %>!</h1>
                <p>Aquí puedes ver tus votos y participar en las categorías activas</p>
            </div>

            <%- include('../partials/message') %>

            <div class="dashboard-stats">
                <div class="stat-card">
                                        <div class="stat-info">
                        <h3><%= myVotes.length %></h3>
                        <p>Votos Realizados</p>
                    </div>
                </div>
                <div class="stat-card">
                                        <div class="stat-info">
                        <h3><%= activeCategories %></h3>
                        <p>Categorías Activas</p>
                    </div>
                </div>
                <div class="stat-card">
                                        <div class="stat-info">
                        <h3><%= activeCategories - myVotes.length %></h3>
                        <p>Pendientes por Votar</p>
                    </div>
                </div>
            </div>

            <div class="section">
                <div class="section-header">
                    <h2>Categorías Disponibles</h2>
                    <a href="/categories" class="btn btn-primary">Ver Todas</a>
                </div>

                <% if (categories.length === 0) { %>
                    <div class="empty-state">
                        <p>No hay categorías activas en este momento</p>
                    </div>
                <% } else { %>
                    <div class="categories-grid">
                        <% categories.slice(0, 6).forEach(category => { %>
                            <div class="category-card">
                                <h3><%= category.title %></h3>
                                <p class="category-description"><%= category.description || 'Sin descripción' %></p>
                                <div class="category-info">
                                    <span class="info-item">
                                        <strong><%= category.options.length %></strong> opciones
                                    </span>
                                    <span class="info-item">
                                        <strong><%= category.options.reduce((sum, opt) => sum + opt.votes, 0) %></strong> votos
                                    </span>
                                </div>
                                <% const hasVoted = myVotes.some(vote => vote.category._id.toString() === category._id.toString()); %>
                                <% if (hasVoted) { %>
                                    <button class="btn btn-secondary btn-block" disabled>
                                        Ya votaste
                                    </button>
                                <% } else { %>
                                    <a href="/categories/<%= category._id %>" class="btn btn-primary btn-block">
                                        Votar Ahora
                                    </a>
                                <% } %>
                            </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>

            <div class="section">
                <h2>Mi Historial de Votos</h2>

                <% if (myVotes.length === 0) { %>
                    <div class="empty-state">
                        <p>Aún no has votado en ninguna categoría</p>
                        <a href="/categories" class="btn btn-primary">Explorar Categorías</a>
                    </div>
                <% } else { %>
                    <div class="votes-list">
                        <% myVotes.forEach(vote => { %>
                            <div class="vote-item">
                                <div class="vote-info">
                                    <h4><%= vote.category.title %></h4>
                                    <p class="vote-date">
                                        Votado el <%= new Date(vote.votedAt).toLocaleDateString('es-ES', {
                                            year: 'numeric',
                                            month: 'long',
                                            day: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </p>
                                </div>
                                <span class="badge badge-success">Completado</span>
                            </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <footer class="footer">
        <p>&copy; 2026 ICG400 Sistema de Votación</p>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>
