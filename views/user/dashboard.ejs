<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Dashboard - ICG400</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar', { user: user }) %>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h1>¡Bienvenido, <%= user.username %>!</h1>
                <p>Participa en las votaciones de ICG400</p>
            </div>

            <%- include('../partials/message') %>

            <!-- Sección: Cómo Funciona -->
            <div class="how-it-works-section">
                <div class="how-it-works-card">
                    <h2>¿Cómo funciona ICG400?</h2>

                    <div class="how-it-works-steps">
                        <div class="step">
                            <div class="step-number">1</div>
                            <div class="step-content">
                                <h4>Propón tus incógnitas</h4>
                                <p>Tienes hasta el <strong>domingo 25 de enero</strong> para enviar tus sugerencias de incógnitas, nada de putos trolleos cabrones</p>
                            </div>
                        </div>

                        <div class="step">
                            <div class="step-number">2</div>
                            <div class="step-content">
                                <h4>Opciones fijas</h4>
                                <p>Todas las incógnitas tendrán las mismas 4 opciones: <strong>Gringo</strong>, <strong>Marco</strong>, <strong>Alex</strong> y <strong>Joak</strong>.</p>
                            </div>
                        </div>

                        <div class="step">
                            <div class="step-number">3</div>
                            <div class="step-content">
                                <h4>Revisión de moderadores</h4>
                                <p>Las propuestas serán aceptadas o rechazadas según el criterio de Rufer y Jam</p>
                            </div>
                        </div>

                        <div class="step">
                            <div class="step-number">4</div>
                            <div class="step-content">
                                <h4>Período de votación</h4>
                                <p>Una vez finalice el plazo de sugerencias, podrás votar en todas las incógnitas que hayan sido aprobadas.</p>
                            </div>
                        </div>
                    </div>

                    <div class="how-it-works-cta">
                        <a href="/propose" class="btn btn-secondary">Proponer Incógnita</a>
                    </div>
                </div>
            </div>

            <div class="dashboard-stats">
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= myVotes.length %></h3>
                        <p>Votos Realizados</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= activeCategories %></h3>
                        <p>Incógnitas Totales</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= activeCategories - myVotes.length %></h3>
                        <p>Pendientes por Votar</p>
                    </div>
                </div>
            </div>

            <!-- Sección de Iniciar Votación / Ver Resultados -->
            <div class="start-voting-section">
                <% if (!hasEnded) { %>
                    <!-- Período activo: mostrar votación -->
                    <% if (activeCategories === 0) { %>
                        <div class="voting-card voting-empty">
                            <div class="voting-icon">📋</div>
                            <h2>No hay votaciones activas</h2>
                            <p>Vuelve más tarde para participar en las votaciones</p>
                        </div>
                    <% } else if (myVotes.length >= activeCategories) { %>
                        <div class="voting-card voting-complete">
                            <div class="voting-icon">🎉</div>
                            <h2>¡Has completado todas las votaciones!</h2>
                            <p>Gracias por participar en ICG400</p>
                            <a href="/categories" class="btn btn-secondary">Ver tu progreso</a>
                        </div>
                    <% } else { %>
                        <div class="voting-card voting-ready">
                            <div class="voting-icon">🗳️</div>
                            <h2>Votaciones Abiertas</h2>
                            <p>Hay <strong><%= activeCategories - myVotes.length %></strong> incógnitas esperando tu voto</p>
                            <a href="/categories" class="btn btn-primary btn-lg btn-start-voting">
                                <span class="btn-icon">→</span> Iniciar Votación
                            </a>
                        </div>
                    <% } %>
                <% } else { %>
                    <!-- Período terminado: mostrar resultados -->
                    <div class="voting-card voting-results">
                        <div class="voting-icon">🏆</div>
                        <h2>¡Período Finalizado!</h2>
                        <p>Los resultados han sido compilados. ¡Descúbrelos!</p>
                        <a href="/results" class="btn btn-primary btn-lg">
                            <span class="btn-icon">→</span> Ver Resultados
                        </a>
                    </div>
                <% } %>
            </div>

            <div class="section">
                <h2>Mi Historial de Votos</h2>

                <% if (myVotes.length === 0) { %>
                    <div class="empty-state">
                        <p>Aún no has votado en ninguna incógnita</p>
                        <p class="text-muted">Inicia la votación para comenzar a participar</p>
                    </div>
                <% } else { %>
                    <div class="votes-list">
                        <% myVotes.forEach(vote => { %>
                            <div class="vote-item">
                                <div class="vote-info">
                                    <h4><%= vote.category.title %></h4>
                                    <p class="vote-date">
                                        Votado el <%= new Date(vote.votedAt).toLocaleDateString('es-ES', {
                                            year: 'numeric',
                                            month: 'long',
                                            day: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </p>
                                </div>
                                <span class="badge badge-success">Completado</span>
                            </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <footer class="footer">
        <p>&copy; 2026 ICG400 Sistema de Votación</p>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>
