<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Dashboard - ICG400</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar', { user: user }) %>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h1>¡Bienvenido, <%= user.username %>!</h1>
                <p>Participa en las votaciones de los Premios ICG400</p>
            </div>

            <%- include('../partials/message') %>

            <div class="dashboard-stats">
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= myVotes.length %></h3>
                        <p>Votos Realizados</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= activeCategories %></h3>
                        <p>Categorías Totales</p>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-info">
                        <h3><%= activeCategories - myVotes.length %></h3>
                        <p>Pendientes por Votar</p>
                    </div>
                </div>
            </div>

            <!-- Sección de Iniciar Votación -->
            <div class="start-voting-section">
                <% if (activeCategories === 0) { %>
                    <div class="voting-card voting-empty">
                        <div class="voting-icon">📋</div>
                        <h2>No hay votaciones activas</h2>
                        <p>Vuelve más tarde para participar en las votaciones</p>
                    </div>
                <% } else if (myVotes.length >= activeCategories) { %>
                    <div class="voting-card voting-complete">
                        <div class="voting-icon">🎉</div>
                        <h2>¡Has completado todas las votaciones!</h2>
                        <p>Gracias por participar en los Premios ICG400</p>
                        <a href="/categories" class="btn btn-secondary">Ver tu progreso</a>
                    </div>
                <% } else { %>
                    <div class="voting-card voting-ready">
                        <div class="voting-icon">🗳️</div>
                        <h2>Votaciones Abiertas</h2>
                        <p>Hay <strong><%= activeCategories - myVotes.length %></strong> categorías esperando tu voto</p>
                        <a href="/categories" class="btn btn-primary btn-lg btn-start-voting">
                            <span class="btn-icon">→</span> Iniciar Votación
                        </a>
                    </div>
                <% } %>
            </div>

            <div class="section">
                <h2>Mi Historial de Votos</h2>

                <% if (myVotes.length === 0) { %>
                    <div class="empty-state">
                        <p>Aún no has votado en ninguna categoría</p>
                        <p class="text-muted">Inicia la votación para comenzar a participar</p>
                    </div>
                <% } else { %>
                    <div class="votes-list">
                        <% myVotes.forEach(vote => { %>
                            <div class="vote-item">
                                <div class="vote-info">
                                    <h4><%= vote.category.title %></h4>
                                    <p class="vote-date">
                                        Votado el <%= new Date(vote.votedAt).toLocaleDateString('es-ES', {
                                            year: 'numeric',
                                            month: 'long',
                                            day: 'numeric',
                                            hour: '2-digit',
                                            minute: '2-digit'
                                        }) %>
                                    </p>
                                </div>
                                <span class="badge badge-success">Completado</span>
                            </div>
                        <% }); %>
                    </div>
                <% } %>
            </div>
        </div>
    </main>

    <footer class="footer">
        <p>&copy; 2026 ICG400 Sistema de Votación</p>
    </footer>

    <script src="/js/main.js"></script>
</body>
</html>
