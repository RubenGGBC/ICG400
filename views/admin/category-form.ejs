<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= category ? 'Editar' : 'Nueva' %> Categoría - Admin</title>
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <%- include('../partials/navbar', { user: user }) %>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h1><%= category ? 'Editar' : 'Nueva' %> Categoría</h1>
                <a href="/admin/categories" class="btn btn-secondary">← Volver</a>
            </div>

            <%- include('../partials/message') %>

            <div class="form-container">
                <form
                    action="<%= category ? '/admin/categories/' + category._id + '/update' : '/admin/categories/create' %>"
                    method="POST"
                    class="category-form"
                >
                    <div class="form-group">
                        <label for="title">Título de la Categoría *</label>
                        <input
                            type="text"
                            id="title"
                            name="title"
                            required
                            placeholder="Ej: Mejor Jugador 2024"
                            value="<%= category ? category.title : '' %>"
                        >
                    </div>

                    <div class="form-group">
                        <label for="description">Descripción</label>
                        <textarea
                            id="description"
                            name="description"
                            rows="3"
                            placeholder="Descripción de la categoría (opcional)"
                        ><%= category ? category.description : '' %></textarea>
                    </div>

                    <div class="form-group">
                        <label>
                            <input
                                type="checkbox"
                                name="allowMultipleVotes"
                                <%= category && category.allowMultipleVotes ? 'checked' : '' %>
                            >
                            Permitir votos múltiples
                        </label>
                    </div>

                    <div class="form-group">
                        <label>
                            <input
                                type="checkbox"
                                name="isActive"
                                <%= !category || category.isActive ? 'checked' : '' %>
                            >
                            Categoría activa
                        </label>
                    </div>

                    <div class="form-group">
                        <label>Opciones de Votación (Fijas)</label>
                        <div class="fixed-options-info">
                            <p class="form-hint">Las opciones para todas las categorías son:</p>
                            <ul class="fixed-options-list">
                                <li>Gringo</li>
                                <li>Marco</li>
                                <li>Alex</li>
                                <li>Joak</li>
                            </ul>
                            <p class="form-hint">Estas opciones se agregarán automáticamente a la categoría.</p>
                        </div>
                    </div>

                    <div class="form-actions">
                        <a href="/admin/categories" class="btn btn-secondary">Cancelar</a>
                        <button type="submit" class="btn btn-primary">
                            <%= category ? 'Actualizar' : 'Crear' %> Categoría
                        </button>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <footer class="footer">
        <p>&copy; 2026 ICG400 Sistema de Votación</p>
    </footer>

    <script src="/js/category-form.js"></script>
</body>
</html>
